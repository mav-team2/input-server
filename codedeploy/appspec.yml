version: 0.0
os: linux
files:
  - source: /
    destination: /home/ubuntu
permissions:
  - object: /
    pattern: "**"
    owner: ubuntu
    group: www-data
hooks:
  ApplicationStart:
    - location: deploy.sh
      timeout: 300
      runas: ubuntu